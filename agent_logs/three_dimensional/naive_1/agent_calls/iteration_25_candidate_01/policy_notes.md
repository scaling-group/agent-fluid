# Candidate diagnosis and hypothesis

## Evidence read before editing

- All four sampled rollouts use direct uniform still-water initialization with
  `U_infinity=(0,0,0)` and terminate in capture. The combined sheets show
  genuine self-propulsion: each top-down row develops a coherent alternating
  target-bending street, while each oblique row retains compact paired caudal
  Lambda2 structures through approach. There is no sampled visual failure in
  this allocation; the inherited `0.81206L` near miss followed by a left exit
  is therefore the informative failure boundary, not a visually observed
  member of the four-sheet comparison.
- The executable-identical common-envelope redistribution samples capture at
  `18.8265T` and `18.8815T` with mean distance `2.08855L` and `2.08896L`.
  Their replicated distance integral is better than the broadside-reserve
  sample's `2.09386L`, but inherited evidence also contains an
  executable-equivalent redistribution near miss, so the lower integral is
  not yet robust.
- The broadside-reserve sample captures earlier at `18.7055T` and preserves
  the same two-view wake. Reconstructed inherited diagnostics place its first
  activation at `1.5945L` and its peak added curvature share at `0.03851`
  while the target remains ahead. This is exercised non-interference outside
  the saturated route argument, not proof that it recovers the inherited
  near-miss topology. The rearward-multiplier sample captures at `18.9640T`
  but never exercises its rearward branch and supplies no recovery evidence.
- The broadside sample remains in the captured carrier's joint-contact band,
  although its peak planar force and moment are at the upper edge of current
  samples. The candidate must therefore preserve both curvature shares and
  the traveling wave, rather than add more posterior authority or a raw
  velocity residual.

## Policy hypothesis

Compose exactly the two exercised state-feedback mechanisms: retain the
pre-overshoot broadside mean-curvature reserve outside route saturation, and
restore the common rhythmic-envelope redistribution across displacement-only
half-cycles. The reserve supplies bounded route authority in the forward
broadside sector where the inherited redistribution miss first became
vulnerable; the redistribution retains its replicated lower distance-integral
trajectory away from that sector. No gains, route sign, response-release
logic, posterior lag, or actuator projection change.

The hypothesis is falsified if the rollout loses capture, repeats a broadside
near miss/left exit, loses the alternating top-down or compact oblique caudal
wake, leaves the `2.08855--2.09386L` mean-distance neighborhood without a
semantic benefit, or materially exceeds the inherited joint-contact and
planar-load band. A capture alone is compatibility evidence; recovery requires
later evidence that this composition changes the documented near-miss
topology.

bookshelf_consulted: true
source_domain: robotic-fish CPG turning and classical fish mean-curvature turning
source_mechanism: bounded mean-bend steering combined with observed-half-cycle rhythmic redistribution
transferable_invariant: persistent body-frame route geometry should own turn sign while a small phase-conditioned redistribution preserves the directed traveling bend
nontransferable_details: published gains, duty ratios, species kinematics, dimensional beat frequencies, exact vortex phase, and prescribed routes
policy_translation: combine normalized forward/lateral target fractions for a smooth pre-overshoot curvature reserve with joint-1 displacement phase for a positive common amplitude envelope under the two-joint state-feedback contract
falsification: reject on lost capture, repeated broadside exit, damaged two-view wake coherence, or materially higher joint/load contact; do not infer rescue from an ordinary capture
