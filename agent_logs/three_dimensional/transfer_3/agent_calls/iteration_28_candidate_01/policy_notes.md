# Carrier-demodulated phase-response replication candidate

## Evidence diagnosis recorded before the policy edit

- All four sampled rollouts satisfy the frozen contract: direct uniform
  `U_infinity=(0,0,0)` initialization, no cylinders or prewarm, finite
  dynamics, and capture at `18.6560--18.7440T`. There is no sampled
  non-capture. I therefore compared the best finite sample against the
  assigned parent, the sampled physical-response alternatives, the two later
  inherited rollouts, and the inherited `1.845L` high-pass/left-exit failure
  boundary rather than manufacturing a failure ordering from close scores.
- I inspected the combined sheets for the best-score sample, the assigned
  parent, the phase-demodulated sample, and both inherited amplitude-residual
  rollouts. In every top-down row the fish translates under its own traveling
  body wave and leaves a coherent alternating posterior vorticity street. The
  oblique rows show compact paired three-dimensional Lambda2 structures shed
  behind the body through target closure. Zero imposed flow and local-flow RMS
  `0.01807--0.01844U` confirm self-propulsion rather than passive advection;
  none of the close variants visibly collapses its wake, loops, collides, or
  becomes unstable. The inherited amplitude-residual sheets do show a slower,
  differently bent final approach, consistent with their trajectory cost.
- The actuator-consistent assigned parent captures at `18.6725T`, score
  `-0.13362`, mean distance `2.02129L`, posterior limit occupancy `76.11%`,
  action RMS `24.95/28.85 rad/T^2`, and force/moment RMS
  `0.01350/0.00703`. An identical-hash inherited repeat at `19.0080T` makes
  small one-off arrival differences unresolved. Raw helpful-moment amplitude
  relief is fastest at `18.6560T`, but its `75.74%` posterior occupancy and
  `0.01328/0.00691` loads remain inside the parent replication spread; the
  stress-gated raw-moment variant is slower at `18.7440T` and likewise does
  not separate.
- Carrier-demodulated moment modulation of posterior phase captures at
  `18.7165T`, inside the route timing spread, while lowering posterior
  occupancy to `74.14%`, action RMS to `24.46/28.56 rad/T^2`, and
  force/moment RMS to `0.01309/0.00682`. Its mean distance `2.02337L` is
  slightly worse, so the positive claim is load shedding, not faster routing.
  This is the only sampled physical-response change with a material effort
  result and it has not yet been replicated.
- The assigned parent's subsequent residual-allocation hypothesis is now
  falsified. Giving carrier-demodulated helpful moment only half-cycle
  amplitude ownership preserves capture and both wake views, but the two
  inherited implementations slow to `18.9750T` and `19.1070T`, raise mean
  distance to `2.04497L` and `2.05106L`, and score only `-0.15631` and
  `-0.16256`. Posterior occupancy falls to `74.20--74.84%` and loads to about
  `0.01312--0.01326/0.00683--0.00691`, so lower effort alone did not preserve
  the route. Later workers should not scalar-tune or repeat this amplitude
  allocation.

## Policy hypothesis recorded before editing

Replicate the sampled carrier-demodulated phase-response policy exactly. Keep
the normalized bearing-plus-LOS-rate route, recoil-conditioned yaw response,
continuous two-joint C-bend, state-feedback traveling carrier,
response-reversing half-cycle steering, persistent same-side actuator gate,
coefficient-norm-preserving phase rotation, and componentwise feasibility
projection. Subtract the sampled reflection-odd two-joint carrier estimate
from normalized yaw moment, then let only the bounded residual modulate the
already stress-gated posterior phase recruitment bidirectionally. This changes
neither the route geometry nor the carrier and directly tests whether the one
material load result survives rollout variability after two amplitude-channel
failures.

Support requires capture no later than the replicated half-cycle bound near
`19.052T`, mean distance no greater than the prior phase-residual result
`2.02337L`, coherent top-down and oblique wakes, posterior occupancy near or
below `74.3%`, and force/moment RMS near or below `0.01317/0.00685`. A score or
arrival change inside the known parent spread is not an improvement by itself.
Falsify the mechanism if it loses capture, leaves those route bounds, or does
not reproduce the material effort/load separation; in that case retain the
plain actuator-consistent phase policy and avoid further moment allocation in
this low-flow still-water lane until a genuinely beat-scale response signal is
available.

bookshelf_consulted: true
source_domain: wake-adaptive swimming and sensor-modulated robotic-fish rhythmic control
source_mechanism: preserve the propulsive traveling rhythm while separating joint-phase carrier response from a small sensed hydrodynamic residual
transferable_invariant: slow body-frame route guidance and the primary rhythmic carrier must remain intact while only a bounded carrier-demodulated response adjusts an existing steering actuator
nontransferable_details: published gains, dimensional frequencies, species or robot kinematics, full-body waveforms, exact vortex phases, organized-wake synchronization, and task-specific routes
policy_translation: use normalized target bearing and LOS rate for route demand; subtract a reflection-odd estimate formed from normalized two-joint angle and velocity from normalized yaw moment; use residual alignment only to modulate stress-gated posterior phase recruitment
falsification: reject if capture exceeds 19.052T, mean distance exceeds 2.02337L, either wake view loses coherence, posterior occupancy is not near or below 74.3%, or force/moment RMS is not near or below 0.01317/0.00685

The current candidate's CFD outcome is not claimed here; it becomes evidence
only after this worker exits.
