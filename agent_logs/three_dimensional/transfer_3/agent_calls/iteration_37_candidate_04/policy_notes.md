# Radial-approach carrier-damping candidate

## Evidence diagnosis recorded before the policy edit

- All four sampled rollouts satisfy the frozen direct-uniform still-water
  contract: `U_infinity=(0,0,0)`, no prewarm, no cylinders, finite dynamics,
  and capture at `18.6560--18.7330T`. I inspected the best exact-policy repeat
  and the joint-rate anti-windup counterexample from release to capture in both
  rows of their combined sheets. Top-down views show body-led translation and
  a coherent alternating caudal-vorticity street by `4T`; oblique views show
  compact Lambda2 structures shed behind the moving tail. The anti-windup wake
  and route are visually indistinguishable from the exact repeat, with no
  passive advection, wake breakup, collision, boundary contact, or instability.
- The exact actuator-consistent policy captures at `18.6725T` and `18.7330T`
  in the current samples, with scores `-0.13219` and `-0.13142`. Joint-rate
  anti-windup captures at `18.7000T` and `-0.13320`; its posterior acceleration-
  limit occupancy is `73.91%`, but exact repeats span `75.19--75.46%` and all
  force/moment RMS values overlap at about `0.0133/0.0069`. Helpful-moment
  relief captures at `18.6560T`, but the inherited exact-policy variability and
  failed repeat already rule out its `0.0165T` one-run edge as robust evidence.
- The inherited closing-speed carrier envelope is the informative negative.
  It contracts the anterior Van der Pol amplitude parameter by at most `20%`
  only near an already-fast approach, retains coherent wakes in both views,
  and still captures, but arrival slows to `18.8705T`, mean distance rises to
  `2.04281L`, and score falls to `-0.15464`. Posterior occupancy and force/moment
  RMS fall to `74.06%` and `0.01312/0.00683`, which overlap the current sampled
  envelope and do not establish a route or effort advantage. This is consistent
  with the edit acting only through the small `mu=0.35` nonlinear oscillator
  injection while leaving the dominant restorative and clipped tail terms
  nearly unchanged; another amplitude-limit scalar is not a useful next test.
- Across the current traces, range falls from `2.69--2.71L` near `16T` to
  `1.22--1.25L` near `18T` while rigid-body radial closing remains
  `0.72--0.76U`. Posterior clipping persists and both wakes remain productive.
  The evidence supports testing a direct, smoothly gated carrier-energy sink,
  while the inherited near-range curvature-allocation loop rules out coasting
  or withdrawing LOS, mean-curvature, half-cycle, or phase authority.

## Policy hypothesis recorded before the policy edit

Preserve the evaluated normalized bearing-plus-LOS-rate route, distributed
C-bend, response-reversing half-cycle steering, persistent same-side posterior
phase recruitment, and feasible-action projection. Reuse the inherited
rotation-invariant range/closing/target-forward approach gate, but apply it to
one distinct actuator: a bounded viscous term opposing anterior joint velocity
about the existing route center. The route-center stiffness, frequency,
posterior lag, mean tail curvature, half-cycle scale, phase rotation, and
physical limits remain unchanged. Unlike the evaluated amplitude-parameter
contraction, this term directly dissipates carrier kinetic energy and should
reduce the tail demand generated by the anterior wave during the last few
beats without introducing a clock, stage, route, or target-specific coordinate.

Support requires capture with coherent wakes in both views, arrival no later
than `19.052T`, no early speed or wake loss, and a posterior-occupancy or load
reduction that separates below the exact/anti-windup overlap without shifting
saturation upstream. Falsify on a near-pass loop, delayed/lost capture, weaker
far-field propulsion, route-center lag, or effort remaining inside the sampled
`73.91--75.46%` posterior occupancy and `0.01312--0.01335` force-RMS envelope.

An offline replay of only the new memoryless gate and damping expression on
the two completed exact traces checks scope without predicting altered CFD.
The gate first exceeds `0.01` at `15.19--15.21T` and about `3.30L`; damping is
only `0.58--0.87 rad/T^2` near `16T` and peaks at `4.57--4.63 rad/T^2` after
`17.4T`, versus the `31.42 rad/T^2` action limit. Thus the candidate is exactly
the plain evaluated route during early wake formation and makes a bounded,
late, dynamically non-negligible intervention.

bookshelf_consulted: true
source_domain: sensor-modulated robotic-fish CPG control and terminal capture control
source_mechanism: preserve target feedback and a directed traveling bend while continuously dissipating surplus rhythmic energy during a fast near approach
transferable_invariant: normalized target range and rotation-invariant radial closing speed may gate a bounded energy sink without withdrawing route or steering authority
nontransferable_details: published damping or CPG gains, dimensional frequencies, species or robot kinematics, prescribed gait envelopes, exact vortex phases, and task-specific routes
policy_translation: retain the two-joint LOS and phase controller; use body-frame target geometry and rigid velocity to gate a bounded term opposing anterior joint velocity about the existing route center
falsification: reject on delayed or lost capture, weakened early wake or propulsion, a repeated near-pass loop, route-center lag, upstream saturation transfer, or effort changes within exact-policy variability

The current candidate's CFD evaluation occurs only after this worker exits and
is not used as evidence here.
