# Controlled replication of helpful-response half-cycle relief

## Evidence diagnosis recorded before the policy edit

- All four sampled evaluations satisfy the frozen contract: direct uniform
  `U_infinity=(0,0,0)` initialization, no prewarm or cylinders, finite
  dynamics, and capture at `18.6560--18.8045T`. The informative relative
  failure is therefore the assigned-parent one-sided response allocator's
  failure to replicate its effort result, not a fabricated termination
  ordering among successful trajectories.
- I inspected the combined sheet for the best finite helpful-moment sample and
  the slower one-sided carrier-residual sample from release through capture.
  Both top-down rows show body-led translation and a coherent alternating
  caudal vorticity street. Both oblique rows show compact three-dimensional
  Lambda2 structures shed behind the moving fish through target closure.
  Neither shows passive advection, growing wasteful sway, wake collapse,
  collision, boundary approach, or numerical instability. Direct-zero flow
  and local-flow RMS `0.01808U` and `0.01801U` agree with self-propulsion, so
  the visually similar wakes do not establish an allocator ranking.
- The actuator-consistent prefill captures at `18.6725T`, score `-0.13362`,
  mean distance `2.02129L`, posterior acceleration-limit occupancy `76.11%`,
  action RMS `24.95/28.85 rad/T^2`, and force/moment RMS
  `0.01350/0.00703`. Raw helpful-moment half-cycle relief is the current best
  finite sample at `18.6560T`, score `-0.13321`, mean distance `2.02115L`,
  `75.74%` posterior occupancy, `24.71/28.77 rad/T^2` action RMS, and
  `0.01328/0.00691` force/moment RMS. Every quantity moves in the favorable
  direction, but the `0.0165T` arrival edge is smaller than inherited
  same-policy variability and has only one completed evaluation.
- The assigned-parent one-sided carrier-demodulated allocator first captured
  at `18.8980T` with `74.24%` posterior occupancy and
  `0.01306/0.00680` force/moment RMS. Its exact-policy sampled replication
  captures sooner at `18.8045T`, but occupancy rebounds to `75.93%`, action
  RMS to `25.03/28.79 rad/T^2`, and force/moment RMS to
  `0.01352/0.00704`. It therefore violates the inherited `74.44%` and
  `0.013105/0.006822` replication bounds. This is a captured route, not a
  replicated effort benefit, and another residual scale or instantaneous gate
  is not supported.
- A completed inherited LOS-counter-response anterior-release candidate also
  captures but scores `-0.15658`, below every current sampled score. Its
  inherited artifact does not expose matching trajectory diagnostics here, so
  this supports only a route-cost negative result: do not repeat or scalar-tune
  anterior release from the same instantaneous response alignment.

## Policy hypothesis recorded before editing

Replicate the sampled `dogfish3d_helpful_moment_amplitude_relief_v1` policy
exactly. Preserve normalized bearing-plus-LOS-rate guidance, recoil-conditioned
yaw response, continuous two-joint C-bend, the state-feedback traveling
carrier, response-reversing half-cycle steering, persistent same-side actuator
gate, coefficient-norm-preserving posterior phase rotation, and componentwise
feasibility projection. A helpful physical yaw response may relieve only the
redundant half-cycle amplitude as recoil-conditioned yaw error approaches
closure; it cannot suppress the carrier, phase actuator, or mean-curvature
route. This controlled replication changes neither gains nor mechanism and
tests whether the only current sample with simultaneous route, action, and
load improvements survives rollout variability.

Support requires capture inside the inherited `18.6725--19.0080T` successful
band, coherent wakes in both views, mean distance no greater than the prefill's
`2.02129L`, posterior acceleration-limit occupancy below `76.11%`, and
force/moment RMS below `0.01350/0.00703`. A robust positive result should also
reproduce values near the sampled `18.6560T`, `75.74%`, and
`0.01328/0.00691`; otherwise the small edge remains unresolved. Falsify the
allocator if capture is lost, wake coherence degrades, arrival leaves the
successful band, or effort returns to or exceeds the prefill. In that case
later workers should preserve the plain actuator-consistent route and test a
genuinely beat-scale signal when the observation contract supplies one,
instead of another instantaneous moment or anterior-release gate.

bookshelf_consulted: true
source_domain: wake-adaptive swimming and sensor-modulated robotic-fish rhythmic control
source_mechanism: preserve a productive traveling rhythm while allowing the smallest bounded physical-response feedback to avoid opposing or duplicating useful fluid-induced motion
transferable_invariant: slow normalized route guidance and the propulsive carrier remain primary; a helping physical response may relieve only an already redundant steering residual and must release continuously
nontransferable_details: published gains, dimensional frequencies, species or robot kinematics, full-body waveforms, exact vortex phases, organized-wake synchronization, and task-specific routes
policy_translation: keep body-frame bearing and LOS-rate C-bend guidance plus the observed two-joint carrier; use reflection-invariant alignment of yaw demand and normalized yaw moment only to reduce half-cycle amplitude near yaw closure
falsification: reject if replication loses capture, leaves 18.6725--19.0080T, weakens either wake, exceeds 76.11% posterior acceleration occupancy or 0.01350/0.00703 force/moment RMS, or fails to reproduce a route/load separation from the plain actuator-consistent policy

The current candidate's CFD outcome is not claimed here; it becomes evidence
only after this worker exits.
