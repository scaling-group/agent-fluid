# Helpful-moment amplitude-relief replication candidate

## Evidence diagnosis recorded before the policy edit

- Every sampled rollout and both completed assigned-parent rollouts satisfy the
  frozen initialization contract: direct `uniform_direct` quiescent water,
  `U_infinity=(0,0,0)`, no cylinders or prewarm, finite dynamics, and capture.
  There is no termination failure in this cohort, so the informative failure
  is relative route/load regression rather than loss of capture.
- I inspected all four sampled combined sheets and the inherited step-28
  sheet. Their top-down rows show the body translating toward the target while
  shedding a coherent alternating red/blue street; their oblique Lambda2 rows
  confirm compact three-dimensional structures moving behind the fish. The
  route turns continuously into the capture arc without passive advection,
  wake collapse, growing lateral waste, collision, boundary approach, or
  instability. Local-flow RMS of about `0.018U` and monotone distance progress
  agree with the visual diagnosis.
- The sampled helpful raw-moment amplitude allocator captures at `18.6560T`,
  score `-0.13321`, and mean distance `2.02115L`, with anterior/posterior
  acceleration-limit occupancy `40.74%/75.74%` and force/moment RMS
  `0.01328/0.00691`. The actuator-consistent baseline captures at `18.6725T`
  in the sample and `18.7385T` in inherited evidence, so the helpful allocator's
  `0.0165T` lead over the faster repeat is below the baseline's `0.0660T`
  same-policy spread. It is the best finite sampled score, but not yet a
  resolved robust improvement.
- The assigned parent's stress-gated raw-moment relief captures later at
  `18.7825T`, score `-0.13847`, mean distance `2.02651L`, and
  `40.38%/75.61%` limit occupancy. Its completed step-28 opposition-only,
  carrier-demodulated residual also captures with a coherent wake and reduces
  effort to `40.89%/74.19%` occupancy, `24.42/28.63 rad/T^2` action RMS, and
  `0.01307/0.00680` force/moment RMS, but arrival regresses to `18.8980T`, score
  `-0.14813`, and mean distance `2.03633L`. This is useful load shedding, not
  evidence that more residual phase authority improves the route.

## Policy hypothesis recorded before editing

Replicate the sampled helpful-moment amplitude-relief policy exactly. Preserve
its normalized bearing/LOS-rate route, distributed C-bend, state-feedback
traveling carrier, recoil-conditioned yaw response, persistent same-side
phase recruitment, and acceleration projection. Remove only the assigned
parent's extra `phase_activation` multiplier from helpful-moment relief, so a
helping physical yaw moment may continuously reduce redundant half-cycle
amplitude near yaw-response closure without suppressing the carrier, posterior
mean correction, or coefficient-norm-preserving phase path.

This controlled replication tests whether the best sampled route/load result
survives CFD variability; it does not treat the one-off score edge as already
established. Support requires capture with both wake views coherent, arrival
inside the inherited `18.6560--18.7385T` comparison band, mean distance no
greater than `2.02651L`, posterior limit occupancy no greater than `76.11%`,
and force/moment RMS no greater than `0.01350/0.00703`. Falsify the allocator
if capture is lost, arrival or mean distance leaves those bounds, the wake
weakens, or load exceeds the baseline envelope. Later workers should then
prefer the replicated actuator-consistent route or test genuine beat-scale
history; they should not scalar-tune instantaneous moment or move the residual
onto half-cycle amplitude again.

bookshelf_consulted: true
source_domain: closed-loop robotic-fish CPG and residual CPG/path-following control
source_mechanism: sensor feedback adjusts a bounded redundant steering channel while preserving the productive rhythmic scaffold
transferable_invariant: retain the traveling wave and slow body-frame route loop while observed helpful response relieves only redundant authority after yaw-response closure
nontransferable_details: published gains, dimensional frequencies, species or robot kinematics, exact vortex phases, organized-wake synchronization, and task-specific routes
policy_translation: preserve normalized body-frame bearing and LOS feedback plus observed two-joint phase; use reflection-invariant alignment of yaw-response demand and normalized moment to reduce only half-cycle amplitude without changing primary phase or mean-route authority
falsification: reject if replicated capture leaves 18.6560--18.7385T, mean distance exceeds 2.02651L, posterior limit occupancy exceeds 76.11%, force/moment RMS exceeds 0.01350/0.00703, or either wake view loses coherence
