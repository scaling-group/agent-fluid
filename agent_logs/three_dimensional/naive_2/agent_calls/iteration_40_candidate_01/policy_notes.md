# Counter-bend phase-spring redirect candidate

## Visual and metric diagnosis before editing

All four sampled rollouts satisfy the frozen evidence contract: direct uniform
initialization in still water with `U_infinity=(0,0,0)`, no cylinders, no
prewarm, finite dynamics, and capture termination. I inspected every combined
sheet from release through capture, including the top-down mid-plane vorticity
row and the oblique body/Lambda2 row. Each fish visibly self-propels down-left
on the same direct route behind a coherent alternating planar wake and compact
body-connected three-dimensional vortex train. None shows passive advection,
wake breakup, boundary interaction, or instability. The oscillator, posterior
lag, broad navigation, and response-plus-miss handoff remain useful; the
informative failure is terminal redirect representation and joint exposure.

The score-leading tail-only cubic acceleration asymmetry captures at
`15.1403T/-0.01094`, but crosses with `0.651L` head-relative constant-course
miss, `0/1.269%` anterior/posterior `>40 deg` dwell, and
`0.04041/0.01902` peak normalized planar force/moment. Dual absolute reserve
reduces loads to `0.03872/0.01876` but reaches only `0.556L` miss with
`0/0.898%` dwell at `15.2957T`. Directional two-joint reserve is the best
sampled geometry/reserve compromise: `15.3385T/-0.01502`, `0.461L` miss,
`0/0.681%` dwell, and `0.03959/0.01889` loads. The assigned parent's
envelope-sharing anterior wave-center spillover captures at
`15.2745T/-0.01525` with slightly lower `0.03894/0.01874` loads, but widens
miss to `0.632L` and creates `0.648/1.044%` anterior/posterior dwell. Thus
geometrically contracting amplitude around a shifted center did not protect
the dynamic joint envelope or retain course centering. Together with inherited
posterior-offset, carrier-sign, projection-scalar, force-residual, and
capture-corridor failures, this rejects another offset, phase proxy, reserve
threshold, or scalar-gain iteration.

## Single candidate hypothesis

Return to the sampled directional allocator and preserve its productive
carrier, normalized target/course observations, response-plus-miss release,
tail-first two-joint reserve, pulse, far-field half-cycle navigation, and
smooth acceleration bound. Replace only the terminal cubic
`residual * abs(carrier_acceleration)` actuator on final approach. The existing
smooth `3L` distance gate hands off continuously from that sampled directional
residual to an oscillator-stiffness-scaled phase spring while each receiving
joint is bent opposite the requested redirect. The spring vanishes at joint
center and stays absent throughout the requested-side excursion, so it
initiates a compact state-triggered redirect without weakening the carrier's
restoring half-cycle or imposing a persistent wave center. Tail authority
remains primary; spillover reaches the anterior joint only through the already
evidenced directional angle/rate reserve. The outer predictive route therefore
retains the evaluated actuator and the new representation is localized by an
owned approach state, not a new threshold.

Support requires capture on the direct compact-wake route with course miss at
or below the directional baseline's `0.461L`, zero anterior `>40 deg` dwell,
posterior dwell at or below `0.681%`, peak normalized planar force/moment no
worse than about `0.040/0.019`, and arrival within the `15.14--15.34T` class.
Falsify on lost capture, a wider course, repeated opposite-side pulsing,
increased dwell or loads, slower arrival without a margin gain, changed
far-field behavior, nonfinite action, or loss of reflection equivariance.
Formal CFD is deferred to EvE and is not evidence available to this worker.

bookshelf_consulted: true
source_domain: biological burst turning and robotic-fish sensor-modulated CPG steering
source_mechanism: gate a bounded redirect burst by observed error, beat side, and response while preserving the autonomous traveling carrier
transferable_invariant: recruit extra turn authority only on the counter-bent part of the rhythm and release it into the unchanged restoring half-cycle once the joint reaches center
nontransferable_details: published gains, dimensional beat frequencies, C-start kinematics, hardware duty ratios, linkage geometry, species-specific envelopes, exact vortex phases, task coordinates, routes, and waypoints
policy_translation: retain the sampled directional residual outside the existing smooth near-target distance gate, then map normalized body-frame cubic predicted miss to a signed oscillator-stiffness phase spring on counter-bent joint angle; retain tail-first directional angle/rate reserve and the response-plus-miss handoff, and add no clock or carrier-phase estimate
falsification: reject if capture margin, arrival, direct compact wake, two-joint dwell, normalized loads, boundedness, or reflection equivariance worsens

## Dry validation boundary

The configured guidance-materiality/parameter-schema check, lightweight Julia
policy contract, and solver editable-boundary check pass without running CFD.
A deterministic `78,732`-state grid spanning normalized body-frame target and
course geometry, approach distance, heading response, and both joint angles
and rates produced finite commands inside the owned `30 rad/T^2` smooth
envelope with exact left/right reflection (maximum error `0.0`). The candidate
changed `58,946` grid states from the sampled directional allocator, confirming
an active actuator-representation change. Replay on all four sampled state
histories localized the change as intended: outside `5L`, mean command
difference was below `5e-6 rad/T^2` and maximum difference below
`0.0019 rad/T^2`; inside `3L`, mean difference was
`0.745--0.839 rad/T^2` and maximum difference `6.173 rad/T^2`. These checks
establish schema consistency, finite bounded action, symmetry, outer-route
preservation on recorded states, localization, and activity only. They do not
establish counterfactual capture, wake coherence, course, joint histories,
loads, or arrival; those remain falsifiable in EvE's formal CFD.
