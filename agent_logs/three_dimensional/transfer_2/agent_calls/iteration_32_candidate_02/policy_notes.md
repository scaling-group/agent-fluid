# Velocity-course-response carrier handoff candidate

## Evidence and visual diagnosis before editing

- All four sampled rollouts satisfy the frozen evidence contract: direct
  uniform `U_infinity=(0,0,0)` initialization, no cylinders, no prewarm,
  finite moving-window transport, stable dynamics, and `capture`. Thus there
  is no semantic failure to explain; the useful contrast is the strongest
  finite result against the weakest architecture in the sampled cohort.
- I inspected both rows of every combined keyframe sheet from release through
  capture, using `solver_1d05d22ea1fe` as the strongest finite sample and
  `solver_3fdd63b3fbda` as the informative regression. The top-down rows start
  in blank quiescent water, develop compact alternating caudal-vorticity
  streets, and show continuous self-propelled target-directed arcs. The
  oblique rows show discrete three-dimensional Lambda2 structures shed behind
  the caudal region. None shows advection, collision, wake collapse, domain
  exit, or instability. All four retain a visually similar coherent gait and
  late hook, so vortex prominence alone does not distinguish them.
- Metrics confirm that the approach-restored carrier coupling is the strongest
  tested scaffold. It captures at `15.939T`, scores `0.06189`, and lowers the
  distance integral to `1.82008L`, compared with `16.044T/0.05824/1.82409L`
  for plain redirect priority, `16.088T/0.06072/1.82203L` for globally
  response-released reversal, and `16.247T/0.06033/1.82240L` for redirect
  residual release. It reaches `4/3/2/1L` at
  `11.688/12.909/14.185/15.549T`, earlier than the other three samples, while
  its `13.107L` head path is also shorter than their
  `13.129--13.363L` paths.
- The winning handoff does not purchase progress by leaving the sampled
  physical envelope. Its peak planar-force/yaw-moment coefficients are
  `0.03477/0.01715`, mean absolute anterior/posterior commands are
  `16.66/15.41 rad/T^2`, rate-above-90% residences are `17.70/8.25%`, and
  angle-above-90% residence is zero. Both force peaks are below the plain
  redirect sample's `0.03579/0.01770`, while rate residence remains in the
  established repeat-scale class. Sub-`2L` mean absolute yaw is
  `0.1059 rad/T`, between plain redirect's `0.0943` and global response
  release's `0.1275`, and far below redirect residual release's `0.2690`.
- The positive lesson is continuous regime allocation: response-released
  negative-work reversal is useful while far, but the full coupled carrier
  should be restored through the existing normalized `6--4L` approach
  handoff. The remaining causal weakness is that the far-field release still
  treats yaw aligned with the broad route request as proof that a distinct
  velocity-course redirect is satisfied. Inherited logs already falsify that
  proxy globally: it changed the late hook without a protection benefit, and
  releasing redirect curvature from it produced the slowest, longest path.

## One-candidate policy hypothesis

Start from the evaluated approach-restored carrier-coupling sample and retain
its corrected body-frame target geometry, distance/closing drive relief,
half-cycle steering, posterior allocation, velocity-course redirect,
carrier/steering decomposition, normalized `6--4L` restoration, and bounded
two-acceleration contract. Change only the response certificate used by the
course-redirect carrier governor: measured yaw must have the sign of the
bounded velocity-course redirect command, rather than merely the sign of the
broader route turn, before negative-work reversal is released and the redirect
is considered fulfilled. Keep the evaluated distance handoff so reversal is
still fully coupled before terminal capture.

Expected signature: retain the winner's coherent two-view wake and
`15.94T/1.820L` capture class, while refusing premature far-field carrier
release when route request and velocity-course correction disagree. Falsify
the mechanism if the `10/8/6L` milestones lose the response-release advantage,
if `4/3/2/1L` or capture leave the approach-restored class, or if path, command,
rate/angle residence, force, moment, terminal yaw/slip, joint margin, or either
wake view exceeds the sampled winner/plain-carrier envelope. The new CFD result
occurs only after this worker exits and is not claimed as evidence here.

bookshelf_consulted: true
source_domain: biological C-start redirection and sensor-modulated robotic-fish CPG control
source_mechanism: retain strong bounded directional authority until measured motion satisfies the requested redirect, then release continuously into a coordinated traveling rhythm
transferable_invariant: condition the propulsion-steering authority handoff on observed response to the specific directional request being fulfilled
nontransferable_details: species-specific C-start stages, published gains and cadence, dimensional load thresholds, full-body waveforms, exact vortex phases, and task-specific coordinates or routes
policy_translation: use normalized body-frame velocity-course redirect sign with recent yaw response to release negative-work carrier reversal while far, then restore complete carrier coupling through the already evidenced normalized-distance approach handoff
falsification: reject if early or late milestones regress beyond repeat variation without a material path/load/rate benefit, or if capture, wake coherence, joint margin, bounded commands, force, moment, yaw, or slip deteriorates
