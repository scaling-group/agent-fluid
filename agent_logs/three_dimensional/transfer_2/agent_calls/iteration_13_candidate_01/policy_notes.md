# Candidate diagnosis and hypothesis

## Evidence read before editing

- All four sampled evaluations use direct uniform `U_infinity=(0,0,0)` initialization and terminate in capture. The assigned prefill `solver_1b6df3d71b19` is the strongest finite sample at `19.70649T`, score `-0.215976`, and mean distance `2.105943L`. The weakest sample, `solver_788c33d018cf`, response-releases both anterior and posterior redirect paths and captures later at `19.84951T`, score `-0.220934`, and mean distance `2.111281L`.
- The combined sheets for those two cases were inspected in both rows. Their top-down mid-plane views show self-propelled, alternating coherent wakes and nearly the same smooth target-directed arc; neither shows passive advection, a wake breakup, or a terminal reversal. Their oblique Lambda2 views confirm compact three-dimensional vortex pairs through the approach with no visible instability. The terminal body pose differs only subtly, consistent with the small metric gap rather than a new trajectory topology.
- Cross-checking the traces supports the visual comparison. The prefill versus whole-channel release has mean absolute commands `(18.20,17.02)` versus `(18.09,16.95) rad/T^2`, residence above 90% of the `31 rad/T^2` bound `(35.25%,32.74%)` versus `(34.89%,32.81%)`, zero residence above 90% of the joint-angle limit, and nearly identical peak planar-force/yaw-moment coefficients `0.02458/0.01314` versus `0.02443/0.01298`. The anterior-only release and projected-miss variants also capture at `19.70099T` and `19.72299T` with mean distances `2.106345L` and `2.105653L`, inside this narrow finite band and without a new load or wake class.
- The assigned parent already records a separate completed posterior half-cycle redirect regression (`20.168T`, score `-0.24199`, mean distance `2.13279L`). Together with the current whole-channel, anterior-only, and projected-miss samples, this is negative evidence against another redirect release or scalar geometric blend.
- A different terminal opportunity remains: in the assigned prefill the carrier is still strong at `14T`, but near `16T` and `18T` the sampled accelerations fall to approximately `(-0.77,-1.49)` and `(-0.11,-0.05) rad/T^2` while speed remains about `0.75--0.77U` and the fish is already on the capture arc. The visual wake correspondingly stops adding strong fresh alternating structures near capture. This indicates unused terminal command headroom, not missing curvature authority.

## Policy hypothesis

Preserve the evaluated fore/aft-aware route map, closing-conditioned headroom allocator, LOS-led course redirect, half-cycle steering, and continuous posterior curvature. Add one continuous gait-transition mechanism: while closing inside the approach region, recover a bounded fraction of the suppressed carrier only when the filtered inertial line of sight is nearly stationary. A low LOS rotation while closing is a normalized collision-course observation; if it rotates again, recovery disappears and the existing steering headroom returns. Expected result: maintain a fresh posterior traveling bend during the already-aligned terminal arc and shorten capture/mean distance without replaying a route or releasing the supported steering channel. Falsify if capture is lost, arrival or mean distance leaves the parent repeat band in the wrong direction, the terminal wake becomes disordered, or command/joint/load residence leaves the sampled class.

bookshelf_consulted: true
source_domain: closed-loop robotic-fish CPG modulation and biological C-start-to-propulsive-gait transition
source_mechanism: use sensed directional response to transition continuously from a high-curvature redirect into a propulsive posterior traveling beat
transferable_invariant: restore rhythmic propulsion only after an observed closing course aligns, and withdraw it when the course begins rotating again
nontransferable_details: published oscillator gains, robot or species kinematics, prescribed burst duration, exact vortex phase, and any task-specific route
policy_translation: use bounded approach weight, positive closing speed, and filtered body-relative LOS rotation to partially release the existing amplitude attenuation and carrier damping while leaving both steering paths unchanged
falsification: reject if capture or mean distance regresses beyond repeat variation, if terminal wake coherence does not persist, or if command saturation, joint-limit residence, planar force, or yaw moment leaves the sampled parent class
