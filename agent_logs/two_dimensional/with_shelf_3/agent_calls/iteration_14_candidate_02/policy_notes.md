# Multi-Wake Target-Policy Candidate Notes

## Evidence diagnosis before the policy edit

- The shared prewarm sheet shows the fish held above and downstream of four
  developed interacting vortex streets. It is the certified common initial
  condition, not candidate-specific wake selection or wake-phase robustness.
- All four sampled released sheets reach the target on the same compact
  diagonal: they redirect immediately, sustain a posterior-traveling body
  wake, clear the cylinders, and enter the `0.75L` circle nose first. The best
  scored sample moves at mean velocity `(-0.3362,-0.1404)` against mean local
  flow `(-0.1961,-0.1918)` and displaces its head
  `(-10.9149,-4.2375)L`, confirming active upstream propulsion rather than
  passive advection. No released failure sheet is available, so the two
  successful but dominated combination samples are the strongest current
  adverse visual comparison; inherited downstream exits remain textual
  boundaries only.
- Progress-supervised posterior headroom is the best current finite result. It
  captures at `32.340`, has `1.63773L` mean distance and score `0.234663`, and
  records `65.12/888.56` force/moment RMS. Response-confirmed mean-curvature
  release is slightly faster at `32.136` but scores `0.234607` with higher
  `67.22/907.46` loads. Both retain the direct route and traveling bend, and
  both touch the joint velocity and acceleration ceilings, so the evidence
  does not support more authority.
- Combining those two independently positive withdrawals was reproduced twice
  at exactly `32.4555` release time, `1.64548L` mean distance, score
  `0.226804`, and `67.83/914.09` force/moment RMS. It is worse than each
  component on score, mean distance, and loads, and slower than each. The
  matching keyframes show no compensating topology or clearance benefit. This
  rules out assuming that bounded gates on different controller layers are
  additive and favors one evidenced withdrawal layer at a time.
- The selected branch keeps the mean-curvature request outside the progress
  supervisor. Early padded history, redirection, lateral target-vector motion,
  stalled closure, or recession restores the optional posterior residual;
  coherent closure merely permits direction-selective yielding. The unit-gain
  lagged wave remains active in every case.

## Bookshelf transfer

bookshelf_consulted: true
source_domain: closed-loop robotic-fish CPG residual modulation interpreted through posterior reactive propulsion
source_mechanism: preserve a persistent traveling rhythm while slow goal progress supervises one optional turn-congruent residual
transferable_invariant: slow body-frame target geometry owns route direction, and feedback may withdraw only incremental steering authority without weakening the mean turn request or base traveling wave
nontransferable_details: published gains, dimensional frequencies, species or robot kinematics, exact vortex phases, actuator ratings, source-task routes, and fixed approach distances
policy_translation: use normalized target-window trajectory efficiency to permit direction-selective posterior speed or acceleration pressure to reduce only the optional half-cycle gain; omit the independently acting mean-curvature response-release gate
falsification: reject if direct capture or the compact diagonal topology is lost, arrival or mean distance regresses beyond the sampled progress-supervised result without a load benefit, or held-out wakes expose switching, saturation, or propulsion loss

## Candidate hypothesis

Produce exactly one candidate by promoting the sampled progress-supervised
posterior-headroom branch and removing the unevidenced composition with
response-confirmed mean-curvature release. Preserve filtered body-frame
bearing, the bounded `12 deg` total-curvature request, bearing-conditioned
`40/60 -> 35/65` allocation, anterior state-feedback oscillator, posterior lag
and damping, and maximum `8%` target-helping half-cycle residual.

The one supervisor normalizes history-window closing speed by total
body-frame target-vector rate. Only coherent closure allows posterior
speed/previous-acceleration pressure, normalized by gait-owned scales and
checked for turn-congruent direction, to withdraw the optional residual. It
cannot change mean curvature or suppress the unit-gain traveling wave. This is
an evidence-backed selection from current samples; downstream CFD must still
test the materialized candidate, and no same-worker improvement or held-out
wake robustness is claimed.
