# Multi-wake candidate diagnosis

## Evidence coverage and visual diagnosis

I read the workspace and guidance contracts, the assigned-parent experience,
all four sampled solver scores, observations, compact metrics, nested wake
diagnostics, and policies, plus the assigned parent's inherited optimizer notes
and evaluated descendants. I inspected the shared held-fish prewarm sheet first,
then the released sheets for the assigned `28/27`, gain-`2.0` parent, the
duplicated `28/28`, gain-`2.0` anchor, the sampled `28/28`, gain-`2.1` leader,
and the inherited tail-lag regression. No omitted Bookshelf material,
neighboring configuration, repository history, global-coordinate route, clock,
or external research was used.

The shared prewarm sheet shows the common release condition: the fish is held
above and downstream of four developed, interacting vortex streets, with the
target in the merged second-row wake. This is identical initial-condition
evidence for every candidate, not a candidate-specific wake phase or a route to
memorize.

All available current and inherited keyframe sheets are finite target reaches;
there is no sampled collision, domain-exit, horizon-miss, or unstable sheet to
inspect. The older reversed-sign instability is therefore retained only as an
inherited logged safety boundary. Among the available finite sheets, the
assigned gain-`2.0`, posterior-`27` parent turns into the useful wake but makes
a visibly lower late correction and approaches from beneath. It reaches in
`76.44` release units with score `-0.654416`, `2.555L` mean distance, and
`0.06199` upstream-relative x speed; posterior guard contact does not make the
motion gentle, because command energy is `53200` and RMS force/moment are
`24.69/417.71`.

Restoring the common `28/28` guard at gain `2.0` gives the reproducible compact
physical anchor: a bounded down-left turn, productive lateral beating, diagonal
entry into the developed wake, and target crossing without a loop, collision,
or boundary excursion. It reaches in `74.23`, has `2.561L` mean distance and
`0.06433` upstream-relative x speed, and lowers command energy and RMS
force/moment to `50940` and `22.39/393.08`. Its mean world x velocity
`-0.14682` differs materially from local-flow x `-0.08249`, confirming active
self-propulsion rather than passive advection.

The strongest sampled result changes only oscillator energy gain from `2.0` to
`2.1` on that common-`28` controller. Its sheet preserves the compact diagonal
topology and visibly crosses from a slightly higher, more direct approach. It
improves score to `-0.580586`, mean distance to `2.480L`, arrival to `73.86`,
and upstream-relative x speed to `0.06565`. The gain is not free: command energy
rises to `51797`, mean power proxy to `46.71`, and RMS force/moment to
`24.94/410.68`, while both accelerations touch `28`; nevertheless joint angles
and speeds remain finite and within the hard envelope. The corroborating route,
distance, arrival, and relative-flow improvements make this a stronger result
than a scalar-only gain.

The assigned parent's tail-lag `0.525` descendant is the most informative
failed optimization hypothesis available, though it still reaches the target.
Its sheet makes a deeper lower excursion and returns from beneath; capture
slows to `85.29`, mean distance rises to `2.688L`, upstream-relative x speed
falls to `0.05793`, and RMS force/moment rise to `40.09/558.58`. Thus posterior
phase-response reduction is not supported by its slightly lower mean command
or power, and the evaluated `0.55` tail lag should be retained.

## Single candidate hypothesis

Promote the exact evaluated gain-`2.1`, common-`28` policy as the sole candidate.
Relative to the assigned parent this restores posterior authority from `27` to
`28` and raises only oscillator energy recovery from `2.0` to `2.1`, while
retaining the evidenced `0.75` period, `22 deg` orbit, positive-bearing
gain/cap, tail curvature sharing, `0.55` lag, and `0.65` damping. Using the
exact sampled leader avoids unsupported interpolation on a deterministic wake
whose static gain and guard responses are demonstrably nonlinear.

The expected outcome is finite target capture along the sampled compact
diagonal corridor with approximately the leader's improved distance integral,
arrival, and active upstream-relative propulsion. This is an evidence-backed
promotion, not a claim of universal optimality: reject it if reevaluation loses
capture, takes a lower route branch, or raises effort and load without retaining
the leader's closure benefits. Falsify its applicability if held-out wake phase,
inflow, geometry, or target placement reverses the gain-`2.1` improvement.
